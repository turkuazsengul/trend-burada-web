services:
  trend-burada-local-proxy:
    image: nginx:stable-alpine
    container_name: trend-burada-local-proxy
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped
